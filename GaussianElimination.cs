namespace LinearAlgebraBasics
{
    /// <summary>
    /// Class equipped with methods to handle the operation of Gaussian ELimination on an arbitrary n x m matrix.
    /// </summary>
    public class GaussianElimination
    {
        public static int[,] RowInterchange(int[,] matrix,int row1, int row2)
        {
            int[] Temp = new int[matrix.GetLength(1)];//The row length should equal the number of columns in the matrix.
        } 
    }
}